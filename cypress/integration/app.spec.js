/* eslint-disable */
// Disable ESLint to prevent failing linting inside the Next.js repo.
// If you're using ESLint on your project, we recommend installing the ESLint Cypress plugin instead:
// https://github.com/cypress-io/eslint-plugin-cypress

// Disabled due to cross-origin security error
// describe('Navigation', () => {
//   it('should navigate to the documentation page', () => {
//     // Start from the index page
//     cy.visit('http://localhost:3000/')

//     // Find a link with an href attribute containing "docs" and click it
//     cy.get('a[href*="docs"]').click()

//     // The new url should include "/docs"
//     cy.url().should('include', '/docs')

//     // The new page should contain an h1 with "Getting Started"
//     cy.get('h1').contains('Getting Started')
//   })
// })
